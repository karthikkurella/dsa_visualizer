{
  "name": "dsa-visualizer",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "build": "npm run build --prefix frontend",
    "deploy": "bash deploy.sh"
  },
  "devDependencies": {
    "firebase-tools": "^14.0.0"
  }
}
